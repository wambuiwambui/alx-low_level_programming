#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints lower case latters apart from q and e
 *
 * Return: 0 (succes)
*/
int main(void)
{
	char ch;
	if(ch = 'a' ; ch <= 'z' ; ch++)
	{
		for(ch != 'q' ; ch != 'e')
	}
	{
		putchar('ch');
	}
	putchar ('\n');
	return (0);
	
