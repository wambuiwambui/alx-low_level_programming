#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints all single digit numbers of base ten starting from zero
 *
 * Return: 0(success)
*/
int main(void)
{
	int i;
	for(i = 0 ; i < 10 ; i++)
	{
		printf(%1 ,i);
	}
	printf('\n');
	return (0);
}
